/* eslint-disable @typescript-eslint/no-unused-vars */
import { TNotification } from "types/notifications";

export const getMessage = (notif: TNotification) => {
  return "Admin refunded current hire";
};
